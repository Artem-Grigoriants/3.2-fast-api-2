FastAPI Project with Docker

Этот проект представляет собой FastAPI приложение, упакованное в Docker контейнер. Ниже описана инструкция по настройке окружения, сборке и запуску.

📋 Предварительные требования

Перед началом работы убедитесь, что у вас установлены:

Docker Desktop

Python 3.9+ (для локальной разработки)

Git

🛠 1. Локальная разработка (без Docker)

Если вы запускаете проект локально (на Windows или в WSL/Ubuntu), важно использовать виртуальное окружение.

Настройка окружения

Создайте виртуальное окружение:

# Windows
python -m venv venv
Linux / WSL (Ubuntu)python3 -m venv venv


Активируйте окружение:

# Windows (PowerShell)
.\venv\Scripts\Activate
Linux / WSLsource venv/bin/activate


Установите зависимости:

pip install -r requirements.txt

Примечание: Если в Linux возникает ошибка externally-managed-environment, убедитесь, что вы активировали venv перед установкой.

🐳 2. Запуск через Docker

Docker позволяет запустить приложение в изолированной среде, не устанавливая Python локально.

Важно: Подготовка перед сборкой

Чтобы избежать ошибок при сборке (например, failed to solve: error from sender), убедитесь, что в корне проекта есть файл .dockerignore со следующим содержимым:

venv/
.venv/
env/
__pycache__/
.git/
.idea/

Это предотвратит копирование тяжелой папки виртуального окружения внутрь Docker-образа.

Сборка образа

Соберите Docker-образ, присвоив ему имя (тег), например fastapi-app:

docker build -t fastapi-app .

Запуск контейнера

Запустите контейнер, пробросив порт 8000 с вашего компьютера на порт 8000 внутри контейнера:

docker run -p 8000:8000 fastapi-app

Если порт 8000 занят, можно запустить на другом порту (например, 8080):

docker run -p 8080:8000 fastapi-app

🌐 3. Документация API

После успешного запуска сервера (локально или через Docker), интерактивная документация Swagger UI доступна по адресу:

👉 http://127.0.0.1:8000/docs

Здесь можно тестировать методы API (GET, POST и т.д.) прямо из браузера.

📝 Полезные команды

Остановить все контейнеры:docker stop $(docker ps -q)

Посмотреть список образов:docker images

Посмотреть запущенные контейнеры:docker ps

Очистить индекс Git (если случайно добавили venv):git rm -r --cached .

⚙️ Структура проекта

app/ - Исходный код приложения

Dockerfile - Инструкция для сборки образа

requirements.txt - Список зависимостей Python

.dockerignore - Исключения для Docker (важно для venv)

.gitignore - Исключения для Git